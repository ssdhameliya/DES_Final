pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "DSE-ERP-Mobile-1.2.2-V9.0.92-PREMIUM-UI"

include(":shared")
include(":composeApp")
