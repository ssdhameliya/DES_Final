name: Deploy Jasvi PWA UAT

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build static PWA artifact
        shell: bash
        run: |
          set -euo pipefail
          rm -rf dist
          mkdir -p dist/icons
          cp index.html styles.css config.js manifest.webmanifest sw.js dist/
          cp icons/icon-192.svg icons/icon-512.svg icons/icon-maskable.svg dist/icons/
          cat payload/app.part*.b64 | base64 --decode > dist/app.js
          base64 --decode payload/apple-touch-icon.b64 > dist/icons/apple-touch-icon.png
          test -s dist/app.js
          test -s dist/icons/apple-touch-icon.png
          node --check dist/app.js
          grep -q "api-uat.jasviindustries.in" dist/config.js
          grep -q "enableProdSwitch: false" dist/config.js

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload static PWA
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
